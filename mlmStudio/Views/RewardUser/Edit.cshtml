@model mlmStudio.Models.RewardUser

@{
    ViewBag.Title = "Edit";
    if (Ajax.IsThisAjaxRequest())
    {
        Layout = null;
    }
}

<script src="~/Content/TextEditor/jquery-te-1.4.0.min.js"></script>
<link href="~/Content/TextEditor/jquery-te-1.4.0.css" rel="stylesheet" />
<script> 
    $(function () { 
       $('.ckeditor').jqte();
    });
</script>
@Html.StarkAjaxFormSubmiter("frmRewardUser", "tbRewardUser", true, "Action Successfully Executed")

 
@{
    if (!Ajax.IsThisAjaxRequest())
    {
        <section class="content-header">
            <h1>
                @ViewBag.Title
                <small>@ViewBag.Title</small>
            </h1>
        </section>
    }
}

<script type="text/javascript">
        $(function () { 
            $('select').searchableOptionList({
                maxHeight: '250px'
            });
        });
</script>

@using (Html.BeginForm("Edit", "RewardUser", FormMethod.Post, new { enctype = "multipart/form-data",id = "frmRewardUser"  }))
{
    @Html.AntiForgeryToken()

        <!-- Main content -->
    <section class="content">
        <!-- Small boxes (Stat box) -->
        <div class="row">
            <div class="col-xs-12">
                <div class="box box-solid box-primary">
                    <div class="box-header">
                        <h3 class="box-title">Edit RewardUser</h3>
                    </div><!-- /.box-header -->

                    <div class="alert alert-success alert-dismissable" id="dvMsgRewardUser" style="display:none;">
                        <i class="fa fa-apple"></i>
                        <b> Update Successfully. </b>
                    </div>
                    <!-- form start -->
  
                    <div role="form">
                        <div class="box-body">
                              

                            <div class="form-group">
                                @Html.LabelFor(model => model.UserId, new { @class = "" })
                                @Html.DropDownList("UserId"," -Select- ") 
                                @Html.ValidationMessageFor(model => model.UserId)
                                @Html.StarkDropDownAjaxLink("/User/Create", "AddMore","")
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(model => model.RewardId, new { @class = "" })
                                @Html.DropDownList("RewardId"," -Select- ") 
                                @Html.ValidationMessageFor(model => model.RewardId)
                                @Html.StarkDropDownAjaxLink("/Reward/Create", "AddMore","")
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(model => model.Qty, new { @class = "" })
                                @Html.EditorFor(model => model.Qty) @Html.ValidationMessageFor(model => model.Qty)
                            </div>


                        </div><!-- /.box-body -->
                        <div class="box-footer">
                            @Html.StarkInputButtion("Save","submit","btn btn-primary","") 
                            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

}


